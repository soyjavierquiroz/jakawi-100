import { Head, Link } from '@inertiajs/react';
import AdminLayout from '../layout';
export default function AdminSales({ sales }: { sales:any }) { return <AdminLayout title="Ventas de membresía"><Head title="Ventas de membresía"/><Link className="rounded-md bg-brand px-4 py-3 text-sm font-semibold text-brand-foreground" href="/admin/sales/create">Registrar venta manual</Link><div className="mt-6 grid gap-3">{sales.data.map((sale:any) => <Link key={sale.id} href={`/admin/sales/${sale.id}`} className="rounded-md border border-border bg-surface p-4"><span className="font-semibold">{sale.reference}</span><span className="float-right">Bs{sale.amount}</span><span className="mt-1 block text-sm">{sale.beneficiary.name} · {sale.status}</span><span className="text-xs text-muted-foreground">{sale.collected_by?.name ?? sale.recorded_by?.name ?? 'Admin'} · {sale.payment_channel}</span></Link>)}</div></AdminLayout>; }
