import { Head, Link } from '@inertiajs/react';
export default function PartnerShow({ partner, locations = [], benefits = [], experiences = [] }: any) { return <main className="min-h-screen bg-background p-5 text-foreground"><Head title={partner.name}/><Link href="/">JAKAWI</Link><h1 className="mt-6 text-4xl font-semibold">{partner.name}</h1><p className="mt-3 text-muted-foreground">{partner.description}</p>{partner.whatsapp ? <a className="mt-4 inline-block text-brand" href={`/partners/${partner.slug}/whatsapp`}>WhatsApp</a> : null}<Section title="Lugares">{locations.map((x:any)=><Link className="block py-2" key={x.id} href={`/lugares/${x.slug}`}>{x.name}</Link>)}</Section><Section title="Beneficios">{benefits.map((x:any)=><Link className="block py-2" key={x.id} href={`/beneficios/${x.slug}`}>{x.title}</Link>)}</Section><Section title="Experiencias">{experiences.map((x:any)=><Link className="block py-2" key={x.id} href={`/experiencias/${x.slug}`}>{x.title}</Link>)}</Section></main> }
function Section({title,children}:any){return <section className="mt-8"><h2 className="text-xl font-semibold">{title}</h2><div className="mt-2 rounded-md border border-border p-3">{children || 'Aún no hay contenido disponible.'}</div></section>}
