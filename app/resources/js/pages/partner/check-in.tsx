import { useForm } from '@inertiajs/react'; import PartnerLayout from './layout';
export default function CheckIn({partner}: any) { const form=useForm({code:''}); return <PartnerLayout title="Registrar asistencia" partner={partner}><form className="max-w-sm rounded border p-5" onSubmit={e=>{e.preventDefault();form.post(`/partner/${partner.slug}/asistencias`)}}><label className="grid gap-2">Código de reserva<input className="rounded border p-2" maxLength={6} value={form.data.code} onChange={e=>form.setData('code',e.target.value.toUpperCase())} required/></label><button className="mt-4 rounded bg-brand px-4 py-2 text-brand-foreground">Buscar reserva</button></form></PartnerLayout> }
