import { useForm } from '@inertiajs/react'; import PartnerLayout from './layout';
export default function Review({partner,reservation}:any) { const form=useForm({}); const done=!!reservation.checked_in_at; return <PartnerLayout title="Registrar asistencia" partner={partner}><div className="max-w-md rounded border p-5"><h2 className="text-xl font-semibold">{done?'Asistencia ya registrada':'Revisar asistencia'}</h2><p className="mt-3 font-semibold">{reservation.member_name}</p><p>{reservation.party_size===1?'1 persona':`${reservation.party_size} personas`}</p><p>{reservation.experience}</p><p>{new Date(reservation.starts_at).toLocaleString('es-BO')} · {reservation.venue}</p>{done?<p className="mt-4 text-success">{new Date(reservation.checked_in_at).toLocaleString('es-BO')}</p>:reservation.status==='confirmed'?<button className="mt-4 rounded bg-brand px-4 py-2 text-brand-foreground" onClick={()=>form.post(`/partner/${partner.slug}/asistencias/${reservation.public_id}`)}>Registrar asistencia</button>:<p className="mt-4">Esta reserva no puede registrar asistencia.</p>}</div></PartnerLayout> }
