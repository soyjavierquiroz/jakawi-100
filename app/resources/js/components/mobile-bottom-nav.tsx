import { Link, usePage } from '@inertiajs/react';
import { House, Search, User } from 'lucide-react';
import { cn } from '@/lib/utils';
function JakawiPass() { return <svg viewBox="0 0 24 24" className="size-5" fill="none" stroke="currentColor" strokeWidth="1.9" aria-hidden="true"><path d="M4 7.5A2.5 2.5 0 0 1 6.5 5h11A2.5 2.5 0 0 1 20 7.5v9a2.5 2.5 0 0 1-2.5 2.5h-11A2.5 2.5 0 0 1 4 16.5z"/><path d="M8 9.5h8M8 14.5h5"/></svg>; }
const items = [{ href: '/', label: 'Inicio', icon: House }, { href: '/explorar', label: 'Explorar', icon: Search }, { href: '/mi-jakawi', label: 'Mi JAKAWI', icon: JakawiPass }, { href: '/perfil', label: 'Perfil', icon: User }];
export default function MobileBottomNav() { const url = usePage().url; return <nav aria-label="Navegación principal" className="fixed inset-x-0 bottom-0 z-40 border-t border-border bg-surface/95 px-2 pb-[max(0.5rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur lg:hidden"><div className="mx-auto grid max-w-lg grid-cols-4">{items.map(({ href, label, icon: Icon }) => { const active = href === '/' ? url === '/' : url.startsWith(href) || (href === '/perfil' && url.startsWith('/settings/profile')); return <Link key={href} href={href} aria-current={active ? 'page' : undefined} className={cn('flex min-h-12 flex-col items-center justify-center gap-0.5 text-[11px] font-semibold', active ? 'text-brand' : 'text-muted-foreground')}><Icon className="size-5" aria-hidden="true" />{label}</Link>; })}</div></nav>; }
