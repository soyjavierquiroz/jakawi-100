# Histórico — despliegue

Este documento se conserva como referencia de un procedimiento anterior. Para la arquitectura y runbook vigente, usar [OPERATIONS.md](OPERATIONS.md). En particular, cualquier cambio de frontend requiere reconstruir/recrear `app` y `web` juntos.
